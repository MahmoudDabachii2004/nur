# ============================================================
# NUR — Python Dependencies
# ============================================================

# ----- Core -----
pydantic>=2.7,<3.0
pydantic-settings>=2.3,<3.0
python-dotenv>=1.0,<2.0
requests>=2.32,<3.0
httpx>=0.27,<1.0
tenacity>=8.5,<9.0          # retry with exponential backoff

# ----- Arabic text processing -----
pyarabic>=0.6.5             # tashkeel stripping, normalization
camel-tools>=1.5.2          # Arabic NLP toolkit (optional, for advanced normalization)

# ----- Embeddings (run on Colab) -----
FlagEmbedding>=1.3.0        # BGE-M3 (dense + sparse + ColBERT in one encode())
torch>=2.2.0                # Required by FlagEmbedding
sentence-transformers>=3.0.0  # Fallback / utilities

# ----- Vector DB -----
chromadb>=0.5.5,<0.6.0      # Dev vector DB with metadata filtering

# ----- LLM providers -----
groq>=0.11.0                # Groq Python SDK (Qwen3-32B)
ollama>=0.3.0               # Ollama Python client (local fallback)
openai>=1.40.0              # OpenAI-compatible client (for OpenRouter)
instructor>=1.4.0           # Structured output via Pydantic

# ----- RAG framework -----
langchain>=0.3.0,<0.4.0
langchain-core>=0.3.0
langchain-community>=0.3.0
langchain-groq>=0.1.0
langchain-ollama>=0.1.0
langgraph>=0.2.0            # StateGraph for pipeline orchestration

# ----- Language detection -----
lingua-py>=1.2.0            # Language detection (better than langdetect for AR/EN)

# ----- Backend (Phase 2+) -----
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
sse-starlette>=2.1.0        # Streaming SSE

# ----- CLI -----
rich>=13.7.0                # Beautiful terminal output
typer>=0.12.0               # CLI framework
prompt-toolkit>=3.0.0       # Interactive input

# ----- Evaluation (Phase 11) -----
ragas>=0.2.0

# ----- Testing -----
pytest>=8.0.0
pytest-asyncio>=0.23.0
